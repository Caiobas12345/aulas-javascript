// 1. Declaração da função (o que está na sua imagem)
function nomeDaFuncao(parametro01, parametro02) {
    let resultado = parametro01 + parametro02; // Exemplo de lógica
    return resultado; 
}

// 2. Chamada da função (o que falta no seu código)
console.log(nomeDaFuncao(5, 10));
